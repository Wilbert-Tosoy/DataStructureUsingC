#include <stdio.h>

int main() {
    int start, end;

    printf("Enter the starting number: ");
    scanf("%d", &start);
    printf("Enter the last number: ");
    scanf("%d", &end);

    for (start; start <= end; start++) {
        if (start % 2 != 0) {
            printf("%d ", start);
        }
    }

    return 0;
}
