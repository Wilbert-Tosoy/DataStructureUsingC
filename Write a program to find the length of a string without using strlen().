#include <stdio.h>

int main() {
    char str[100];
    int i = 0;

    printf("Enter the string: ");
    gets(str);

    while (str[i] != '\0') {
        i++;
    }

    int length = i;
    printf("\nString Length: %d", length);

    return 0;
}
