#include <stdio.h>

int main(void) {
    int number;

    printf("Enter a number: ");
    scanf("%d", &number);

    printf("\tDecimal Format: %d\n", number);
    printf("\tOctal Format: %o\n", number);
    printf("\tHexadecimal Format: %x", number);

    return 0;
}
